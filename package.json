{
  "name": "weeker",
  "nx": {
    "targets": {
      "auth:github": {
        "executor": "nx:run-commands",
        "options": {
          "cwd": "{workspaceRoot}",
          "command": "shx run/auth/github"
        }
      }
    }
  },
  "devDependencies": {
    "nx": "^21.2.2",
    "nx-mcp": "^0.3.0"
  }
}
